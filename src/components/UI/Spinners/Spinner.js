import React from "react";
import '../../../styles/UI/Spinners/Spinner.scss';

const Spinner = () => {
    return (
        <div className="Spinner">
            <span className="loader"></span>
        </div>
    )
};

export default Spinner;